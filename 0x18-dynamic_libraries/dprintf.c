#include <stdio.h>

/**
 * dprintf - print message to output
 * @fd: file descriptor of file to print to
 * @format: format string to print
 *
 * Return: 0
 */
int dprintf(int fd, const char *format, ...)
{
	printf("--> Please make me win!\n");
	return (0);
}
